# CollabBoard Client Environment Variables
# In development, Vite automatically proxies /api and /socket.io requests to http://localhost:5000.
# For custom API deployments, define the base API target URL if not using proxy.
# VITE_API_URL=http://localhost:5000
